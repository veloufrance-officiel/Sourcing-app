{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "cp .env.vercel-test .env && npm run vercel-build",
  "outputDirectory": "dist",
  "devCommand": "expo start --web",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    { "source": "/:path*", "destination": "/" }
  ]
}
